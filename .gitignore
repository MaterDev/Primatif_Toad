# --- Primatif_Toad .gitignore (Blacklist Strategy) ---
# Per-context artifacts (shadows, registry, tags) now live in ~/.toad/contexts/,
# so the workspace root no longer needs a whitelist boundary.

# Node tooling
node_modules/

# Build artifacts
target/
**/*.rs.bk

# OS & editor
.DS_Store
*.log
*.swp
*~

# Submodule internal Git state
# Prevents the parent repo from staging submodule history.
crates/*/.git
