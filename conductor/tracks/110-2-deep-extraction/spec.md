# Specification: Deep Extraction & Structured Data (110-2)

## Overview
Enhance the depth of extracted context and provide it in machine-readable JSON formats.

## Sources
- **Strategy:** `docs/releases/v1.1.0/evolution.md` (§ Phase 1)
- **Tasks:** `docs/releases/v1.1.0/tasks.md` (§ Phase 1)

## Requirements
1. Semantic Essence: Include headers and capability markers.
2. Structured Context: Generate `context.json` alongside the markdown manifest.
3. Global `--json` flag support for all CLI commands.
